import {ApplicationState} from "../interfaces/reducers";

export const initialState: ApplicationState = {

};

export function mainReducer(state: ApplicationState, action): ApplicationState {
    return state;
}